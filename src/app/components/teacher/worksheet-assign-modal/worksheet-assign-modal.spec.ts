import{TestBed}from'@angular/core/testing';import{of}from'rxjs';import{WorksheetAssignModal}from'./worksheet-assign-modal';import{httpTestingProviders,routedComponentProviders,verifyHttpRequestsAfterEach}from'../../../../testing/standalone-test-providers';
describe('WorksheetAssignModal duplicate handling',()=>{afterEach(verifyHttpRequestsAfterEach);it('does not silently duplicate and supports both explicit actions',async()=>{await TestBed.configureTestingModule({imports:[WorksheetAssignModal],providers:[...routedComponentProviders(),...httpTestingProviders]}).compileComponents();const fixture=TestBed.createComponent(WorksheetAssignModal),component=fixture.componentInstance;component.classId='class-1';component.selectedId='sheet-1';component.worksheets=[{_id:'sheet-1',title:'Practice'} as any];component.existingAssignments=[{_id:'assignment-1',resourceType:'worksheet',resourceId:'sheet-1',isActive:true} as any];const assign=spyOn((component as any).api,'assignToClass').and.returnValue(of({}));component.submit();expect(component.duplicateAssignment?._id).toBe('assignment-1');expect(assign).not.toHaveBeenCalled();let editId='';component.editExisting.subscribe(id=>editId=id);component.openExisting();expect(editId).toBe('assignment-1');component.show=true;component.duplicateAssignment=component.existingAssignments[0];component.createAnother();expect(assign).toHaveBeenCalledTimes(1);});});
