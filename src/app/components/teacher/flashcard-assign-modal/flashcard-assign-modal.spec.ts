import{TestBed}from'@angular/core/testing';import{of}from'rxjs';import{FlashcardAssignModal}from'./flashcard-assign-modal';import{httpTestingProviders,routedComponentProviders,verifyHttpRequestsAfterEach}from'../../../../testing/standalone-test-providers';
describe('FlashcardAssignModal duplicate handling',()=>{afterEach(verifyHttpRequestsAfterEach);it('offers Edit Existing and creates another only after explicit choice',async()=>{await TestBed.configureTestingModule({imports:[FlashcardAssignModal],providers:[...routedComponentProviders(),...httpTestingProviders]}).compileComponents();const fixture=TestBed.createComponent(FlashcardAssignModal),component=fixture.componentInstance;component.classId='class-1';component.selectedSetId='set-1';component.deadline='2099-09-18';component.sets=[{_id:'set-1',title:'Solar System'} as any];component.existingAssignments=[{_id:'assignment-1',resourceType:'flashcard',resourceId:'set-1',isActive:true} as any];const assign=spyOn((component as any).api,'assignSet').and.returnValue(of({}));component.submit();expect(component.duplicateAssignment?._id).toBe('assignment-1');expect(assign).not.toHaveBeenCalled();let editId='';component.editExisting.subscribe(id=>editId=id);component.openExisting();expect(editId).toBe('assignment-1');component.show=true;component.duplicateAssignment=component.existingAssignments[0];component.createAnother();expect(assign).toHaveBeenCalledTimes(1);});});
